# @name Test technique with variable
# @description Technique with variabled condition
# @version 1.0

bundle agent Test_technique_with_variable
{
  methods:
    "method_call" usebundle => directory_create("${sys.workdir}/module"),
      ifvarclass => "any";
    "method_call" usebundle => file_create("${sys.workdir}/module/env"),
      ifvarclass => "directory_create_${sys.workdir}_module_repaired";
}
